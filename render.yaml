services:
  - type: web
    name: telegram-clone
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && node server-simple.js
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 5005
